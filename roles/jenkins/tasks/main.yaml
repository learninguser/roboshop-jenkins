- name: Include Pipeline tasks
  ansible.builtin.include_tasks: s-pipelines.yml
  vars:
    COMPONENT: "{{item}}"
  loop:
    - frontend
    - catalogue
    - cart
    - user
    - shipping
    - payment